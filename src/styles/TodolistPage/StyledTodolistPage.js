import styled from '@emotion/styled';
import { styledVariables } from '../app/cssMaterial';

const StyledTodolistPage = styled.div`
  position: relative;
  width: 100%;
  height: 100%;
  padding: ${styledVariables.shared.barHeight} 0;
  display: flex;
`;

export default StyledTodolistPage;
